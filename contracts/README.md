# Contracts

This directory will contain the immutable Cotsel settlement protocol, tenant
deployment factory, append-only version registry, public interfaces, deployment
tooling, and contract test suites.

No production contract should be implemented before the settlement
constitution, authority model, state machine, invariants, asset policy, and
deployment model are approved.

# Cotsel SDK

The SDK is the neutral customer integration boundary. It will expose typed
commands, queries, exact monetary amount types, idempotency support, event
schemas, and settlement lifecycle helpers without exposing platform internals.

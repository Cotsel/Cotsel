# Verified Deployments

Each official deployment manifest must record:

- protocol version;
- network and chain ID;
- contract address and runtime code hash;
- approved settlement asset and token address;
- compiler and build provenance;
- constructor arguments and governing authorities;
- audit and verification status;
- activation, deprecation, and final-settlement dates.

Existing settlements remain attached to their originating immutable version.
